name: 4. Build Trio
on:
  workflow_dispatch:

jobs:
  build:
    name: Build Trio
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install iOS 18.4 Runtime
        run: |
          sudo xcode-select -s /Applications/Xcode_16.3.app
          xcodebuild -downloadPlatform iOS -buildVersion 18.4

      - name: Build Unsigned IPA
        run: |
          xcodebuild archive \
            -workspace Trio.xcworkspace \
            -scheme Trio \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath build/Trio.xcarchive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO
          
          mkdir -p build/Payload
          cp -r build/Trio.xcarchive/Products/Applications/*.app build/Payload/
          cd build && zip -r Trio_Unsigned.ipa Payload/

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Trio-App
          path: build/Trio_Unsigned.ipa
